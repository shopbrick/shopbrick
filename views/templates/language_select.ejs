<select class="<%= selectClass || '' %>" onchange="selectLanguage(this.value)">
  <% languages.forEach(({lang, label})=> { %>
    <option value="<%- lang %>"<%- lang===language ? ' selected' : '' %>><%- label %></option>
  <% }) %>
</select>
